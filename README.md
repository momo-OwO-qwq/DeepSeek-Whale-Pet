# DeepSeek 余额小鲸鱼桌宠（Linux 独立版）

![DeepSeek 余额小鲸鱼](assets/DSH2.png)

参考 [MeteorNOX/DeepSeek-Balance-Whale-Widget](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget)（DSH Web 界面右上角余额挂件）设计实现的 **Linux 桌面独立桌宠**：一只可以拖来拖去、按一下会叫、会说随机台词的小鲸鱼，随时提醒你的 DeepSeek 余额与今日消耗。**完全脱离 DSH / 浏览器运行**，只用一行命令启动。

> 原版 DSH 插件源码留档在 [`legacy/dsh-plugin/`](legacy/dsh-plugin/)（不再参与构建）。

## 特性

- 🐋 **透明置顶桌宠**：鲸鱼 PNG 素材 + 自绘 SVG 气泡，无边框、无任务栏条目的浮动窗口，始终置顶
- 💰 **余额监控**：60 秒自动刷新 + 点击鲸鱼手动刷新；余额变化时数字**滚动动画**；瞬时网络抖动自动沿用最近余额不报错
- 📊 **今日已用双模式**（菜单可切换）：
  - **小鲸鱼记账（默认，免令牌）**：每次观测余额后用**余额差值**自动记账，跨天归零归档（保留 30 天）
  - **实时·令牌**：调用 DeepSeek 平台用量接口，按**峰谷定价**实时换算今日已用
- ⏰ **峰谷定价**：高峰 9:00–12:00 与 14:00–18:00（北京时间）；单价：缓存命中 0.05/0.10 元、未命中 1.5/3.0 元、输出 4.5/9.0 元（每百万 token，v4-pro 3 倍价），定价表见 `lib/balance.js`
- 🖱️ **拖拽 + 四分之一吸附**：松手吸附最近边/角，**可自由拖到桌面四边**（渲染进程事件驱动，不依赖系统光标 API）；**左吸附整体水平镜像翻转**（文字同步反向保持可读）
- 🧸 **按压 Q 弹**：按压时鲸鱼压扁变形（底部坐标不变），松手过冲回弹 + **按压/松手音效**（小黄鸭 / 音效1 两套）
- 💬 **随机台词气泡**：点击鲸鱼弹出余额+今日已用；点击气泡切换加权随机台词（峰谷提示/卖萌吐槽/动图），再点关闭，5 秒自动收起
- ✨ **桌宠专属动效**：静止**呼吸动画**、鼠标离开 3 秒**闲置半透明**、低余额**🥺 情绪表情** + **系统通知**、错误 😭 / 加载 💤
- ⚠️ **预警换图**（默认关闭）：余额低于阈值时自动切换**独立预警图**（与主图解耦、可各自上传），并显示红色 `!` 徽标
- 🖼️ **主图替换**：设置里上传本地图片（png/jpg/gif/webp）作为默认显示图，复制到配置目录、随系统保留
- ✏️ **自定义气泡文案**：余额充足 / 预警两种状态分别设置气泡第一行文字（**限 20 字符**）
- 🌗 **暗色主题**：设置面板支持 跟随系统 / 浅色 / 深色，实时生效
- ⚙️ **汉堡菜单**（悬停鲸鱼右上角出现 / 右键鲸鱼）：主题、API Key、平台令牌、大小 0.6–2.5x、音效、音量、用量模式、峰谷文案、气泡、闲置半透明、刷新间隔、低余额阈值、预警换图、主图/预警图上传、气泡文案、开机自启、立即刷新
- 🧰 **系统集成**：托盘图标（显示/隐藏/刷新/设置/自启/退出）、全局热键 `Ctrl+Shift+R` 刷新、**开机自启**（XDG autostart）、**单实例**
- 🔐 配置安全：API Key 保存于 `~/.config/whale-pet/config.json`（目录 0700 / 文件 0600）；支持环境变量 `DEEPSEEK_API_KEY` / `DEEPSEEK_PLATFORM_TOKEN` 覆盖

## 快速开始

```bash
cd DeepSeek-Balance-Whale-Widget-Linux
npm install
npm start
```

启动后小鲸鱼默认出现在**屏幕右下角**。首次使用：右键鲸鱼 → 设置 → 填入 **API Key**（`sk-` 开头），即可显示余额；「今日已用」默认走免令牌的记账模式。

### 打包发布（AppImage / deb）

```bash
npm run dist:appimage   # 或 npm run dist:deb / npm run dist
# 产物在 dist/ 目录
```

AppImage 无需安装直接运行；deb 安装后可从应用菜单启动。

## 使用说明

| 操作 | 效果 |
|---|---|
| 单击鲸鱼 | 弹出气泡（余额 + 今日已用）并刷新 |
| 按住拖动 | 移动鲸鱼；松手吸附最近四分之一边/角 |
| 点击气泡 | 切换随机台词段；再点一次关闭（5 秒自动收起） |
| 右键鲸鱼 / 点击右上角汉堡按钮 | 打开设置窗口 |
| `Ctrl+Shift+R` | 全局刷新余额 |

### 设置项

| 设置 | 说明 |
|---|---|
| **API Key** | 拉取余额必需；留空则鲸鱼提示「未配置」。环境变量 `DEEPSEEK_API_KEY` 优先 |
| **平台令牌** | 「实时·令牌」模式使用（DeepSeek 平台网页会话令牌，可选）；不填自动回落记账模式 |
| **用量模式** | `小鲸鱼记账`（默认，免令牌）/ `实时·令牌` |
| **大小** | 0.6–2.5 倍（默认 1.0，鲸鱼约 190px）；缩放时固定鲸鱼所在角 |
| **音效 / 音量** | 小黄鸭 / 音效1；音量调 0 即静音 |
| **峰谷文案** | 气泡中「当前时间段为：**高峰时段/空闲时段**」的文案风格（默认 / 梁文峰谷 / !?强强?!），可整体开关 |
| **气泡开关** | 关闭后点击鲸鱼不再弹气泡（余额照常刷新） |
| **闲置半透明** | 鼠标离开 3 秒后鲸鱼渐隐为 60% 不透明度 |
| **刷新间隔** | 自动刷新秒数（5–3600，默认 60） |
| **低余额阈值** | 余额低于该值时弹系统通知（30 分钟去重）+ 🥺 表情，默认 10 元 |
| **预警换图** | 开启后余额低于阈值时自动把鲸鱼切换为预警图片，恢复后换回 |
| **主图 / 预警图** | 「选择图片」上传本地图片（自动复制到 `~/.config/whale-pet/images/`，与源文件解耦）；「恢复默认」回到内置素材。主图与预警图彼此独立 |
| **文案·充足 / 文案·预警** | 余额充足 / 预警两种状态下气泡第一行文字，各限 20 字符（超长自动截断） |
| **主题** | 设置面板深色模式：跟随系统 / 浅色 / 深色 |
| **开机自启** | 写入 `~/.config/autostart/deepseek-balance-whale-pet.desktop` |

## 配置与数据

所有数据保存在 `~/.config/whale-pet/`：

```
~/.config/whale-pet/
├── config.json   # 全部设置（含 API Key，权限 600；可用 WHALE_PET_HOME 重定向）
└── usage.json    # 小鲸鱼记账账本（lastBalance / todayUsage / history 30 天）
```

## 目录结构

```text
DeepSeek-Balance-Whale-Widget-Linux/
├── main.js                  # Electron 主进程：窗口/托盘/热键/自启/单实例/IPC/通知
├── preload.js               # contextBridge 安全桥（window.whaleAPI）
├── lib/
│   ├── config.js            # 配置读写（消毒 + 原子写 + 0600 + 环境变量覆盖）
│   ├── ledger.js            # 小鲸鱼记账（余额差值 + 跨天归档）
│   └── balance.js           # 余额/用量拉取、峰谷定价、TTL 缓存、in-flight 去重
├── renderer/
│   ├── pet.html/js/css      # 鲸鱼窗口（UI + 交互 + 动效）
│   └── menu.html/js/css     # 设置窗口
├── assets/                  # 鲸鱼素材与音效（复用原版）
├── test/unit.test.js        # 核心逻辑单元测试（node test）
├── docs/design.md           # 设计文档（架构决策/IPC 协议/几何参数/验证）
└── legacy/dsh-plugin/       # 原版 DSH 插件源码留档（不参与构建）
```

## 验证

```bash
npm test                 # 单元测试：峰谷/定价/记账/配置/选取规则（无需网络）
npm run smoke            # 自动化冒烟测试：窗口/托盘/点击气泡/拖拽跟手截图验证
```

`npm run smoke` 会启动应用 6 秒左右：校验鲸鱼窗口与设置窗口创建、托盘初始化、模拟点击鲸鱼（气泡弹出，通过像素分析验证）、模拟拖拽（窗口跟手到光标钳制位置，误差 < 2px）、真实调用余额接口，并把三张截图写到 `WHALE_PET_HOME`（默认 `$HOME/.config/whale-pet/`）。

手动验证清单：

- [x] 启动后透明窗口显示鲸鱼，置顶于桌面
- [x] 填入 API Key 后显示余额；错误密钥显示「余额接口请求失败: HTTP 401」
- [x] 拖拽移动 + 四边/四角吸附（上边缘已自动化验证贴边）+ 左吸附镜像翻转
- [x] 点击鲸鱼（真实鼠标）弹出气泡 —— 已用 xdotool 真实点击自动化验证
- [x] 点击气泡切换随机台词，再点关闭；按压 Q 弹 + 按压/松手音效
- [x] 呼吸动画 / 闲置半透明 / 低余额通知 / 情绪表情 / 预警换图
- [x] 主图/预警图上传、暗色主题、自定义气泡文案（配置 + 像素级验证）
- [x] 托盘图标 + 全局热键 `Ctrl+Shift+R` + 单实例
- [x] 开机自启写入 `~/.config/autostart/`
- [x] 配置持久化到 `~/.config/whale-pet/`

## 常见问题

- **窗口黑底/不透明**：透明窗口需要桌面合成器（GNOME/KDE/XFCE 默认开启）。若在无合成器的环境运行，可关闭桌面特效或改用普通窗口模式。
- **Wayland 下拖拽不动**：应用已默认强制走 XWayland（`ozone-platform=x11`）以保证窗口定位；如需改回，设置 `ELECTRON_OZONE_PLATFORM_HINT=auto`。
- **托盘图标不显示**：GNOME 需要 AppIndicator 扩展（KDE/XFCE 内置）；没有托盘不影响使用，右键鲸鱼可打开设置。
- **没有声音**：`assets/*.mp3` 必须存在；音量调 0 即静音；某些精简发行版需安装系统音频组件。
- **今日已用显示 --**：记账模式需先完成一次余额观测（点击鲸鱼或等自动刷新）；令牌模式需配置平台令牌。
- **余额不更新**：检查网络与 API Key 有效性；瞬时失败（超时/5xx）时鲸鱼会沿用最近余额不报错。
- **配置放哪**：`~/.config/whale-pet/config.json`；可用环境变量 `WHALE_PET_HOME` 重定向（脚本化/测试用）。

## 与原版差异对照

| 项目 | 原版（DSH Web 插件） | 本 Linux 桌宠版 |
|---|---|---|
| 运行环境 | DSH Web 界面（注入页面） | 独立 Electron 桌面应用 |
| 启动方式 | DSH 页面加载自动注入 | 用户启动 / 开机自启 |
| 定位 | 右下角固定（避让滚动条） | 自由拖拽 + 四分之一吸附 + 位置记忆 |
| 动效 | 按压 Q 弹 | 按压 Q 弹 + **呼吸 + 闲置半透明 + 情绪表情 + 预警换图** |
| 系统集成 | 无 | 托盘 + 全局热键 + 系统通知 + 单实例 |
| 点击命中 | 页面内透明区穿透 | 整窗接收事件，鲸鱼本体外点按被忽略（Linux/XWayland 下 OS 级穿透不可靠，与参考实现一致） |
| 配置存储 | `$DSH_HOME/.dshw-size.json` | `~/.config/whale-pet/config.json` |
| 记账存储 | `$DSH_HOME/.dshw-usage.json` | `~/.config/whale-pet/usage.json` |
| API Key | DSH 凭据服务 | 设置窗口手动填写（或环境变量） |
| 每轮消耗统计 | 监听 DSH 会话事件 | 不适用（独立应用），其余功能 1:1 移植 |

## 许可证

MIT License，见 [LICENSE](LICENSE)。鲸鱼素材与音效沿用原项目。
